var React = require('react');
var Page = require('./components/page.js');
// App
var app = React.createClass({
    render: function () {
        
        return (
        		<Page />
        	);
    }
  
});

module.exports = app;
